// Serif display font for section headings, adopted from the TrustFlow
// visual reference. Additive Google Font (Lora) loaded in index.html —
// scoped to template heading elements only via this style object, so no
// other page's typography is affected.
export const serifHeading = { fontFamily: "'Lora', serif" };
